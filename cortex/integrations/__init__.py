"""cortex.integrations — third-party service auth + clients (Google/Gmail, …).

Service SDKs are imported lazily inside functions so cortex runs without them.
Install Google support with:  pip install ".[google]"
"""
